/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['f-okna.kz'], // Добавьте сюда разрешённые домены для изображений
    },
  };
  
  export default nextConfig;
  